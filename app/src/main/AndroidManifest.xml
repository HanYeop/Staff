<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.example.mobileappproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileAppProject">
        <activity android:name=".dialog.MonthWageActivity"
            android:theme="@style/Theme.CustomDialog"></activity>
        <activity
            android:name=".dialog.ScheduleListActivity"
            android:theme="@style/Theme.CustomDialog" />
        <activity
            android:name=".dialog.ScheduleAddActivity"
            android:theme="@style/Theme.CustomDialog" />

        <activity android:name=".activity.ListActivity" />
        <activity android:name=".activity.registerActivity" />
        <activity android:name=".activity.CalculateActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>